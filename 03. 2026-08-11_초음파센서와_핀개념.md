# 아두이노 학습 노트 — 초음파 센서(HC-SR04)와 핀 개념

> 2026-08-11 / 《처음봐? 아두이노》 진도 + 막혔던 개념 정리

---

## 1. 초음파 센서 HC-SR04

### 원리

소리를 쏘고, 물체에 반사되어 돌아오는 시간을 재서 거리를 계산한다. 산에서 메아리 시간을 재는 것과 같은 원리.

```
거리 = 시간 × 소리속도(340m/s) ÷ 2
```

`÷2`를 하는 이유: 소리가 갔다가 돌아온 **왕복** 거리이므로.

단위 환산 때문에 코드에서는 `* 340 / 2 / 10000` 또는 `/ 58` 같은 형태로 나타난다. 둘은 같은 말이다.

- 340 m/s → 0.034 cm/µs
- 왕복이므로 ÷2 → 0.017 cm/µs
- 그 역수 ≈ 58

### 핀 구성

| 핀 | 역할 |
|---|---|
| VCC | 전원 입력 (5V) |
| GND | 접지 |
| Trig | 아두이노 → 센서. "쏴!" 명령을 받는 핀 |
| Echo | 센서 → 아두이노. 반사파 도착을 알리는 핀 |

### 교재 오류 메모

교재에 초음파를 "약 20MHz"라고 표기했으나 **20kHz가 맞다.** 사람이 들을 수 있는 상한이 20kHz. HC-SR04가 실제로 쏘는 주파수는 40kHz.

---

## 2. GND를 왜 연결하는가

### 막혔던 지점

> "5V는 전기가 나오는 핀이고 GND는 0V니까 아무것도 안 나오는데, 왜 연결해야 하지?"

### 정리

**전기는 한 바퀴 돌아야만 흐른다.** 나가는 길만 있고 돌아오는 길이 없으면 아예 흐르지 않는다.

건전지를 생각하면 된다. +극만 닿게 하고 -극을 안 닿게 하면 리모컨은 작동하지 않는다. 아두이노의 5V가 +극, GND가 -극 역할.

```
아두이노 5V  ──────→  센서 VCC     (나가는 길)
아두이노 GND ←──────  센서 GND     (돌아오는 길)
```

### 더 중요한 것: 기준점

**5V라는 숫자 자체가 GND를 기준으로 잰 값이다.** 전압은 절대량이 아니라 두 지점의 *차이*다. "높이 3m"라고 할 때 바닥을 기준으로 잰 것처럼, GND가 그 "바닥 0m" 역할을 한다.

Trig, Echo 같은 신호선도 마찬가지. 아두이노가 Echo핀을 읽고 "5V다/0V다"를 판단하려면 기준선이 필요하고, 그게 GND다. GND를 연결하지 않으면 전원은 물론이고 신호도 전부 엉터리로 읽힌다.

→ 센서를 여러 개 붙일 때 **"GND는 전부 하나로 묶어라(공통 접지)"** 라는 말의 이유. 모두가 같은 기준점을 봐야 말이 통한다.

---

## 3. INPUT / OUTPUT은 누구 기준인가

### 막혔던 지점

> "Trig는 센서 입장에서 입력인데 왜 코드에는 `OUTPUT`이라고 쓰지?"

```cpp
pinMode(trig, OUTPUT);   // 센서 기준으로는 입력인데?
pinMode(echo, INPUT);
```

### 정리

`pinMode()`는 **아두이노 핀**의 모드를 정하는 함수다. 센서 핀의 모드가 아니다.

선 하나를 두고 양쪽이 이름을 다르게 부르는 것뿐이다.

| 선 | 아두이노 입장 | 센서 입장 |
|---|---|---|
| Trig | OUTPUT (내보냄) | 입력 (받음) |
| Echo | INPUT (읽음) | 출력 (내보냄) |

전화 통화와 같다. 내 입에서 나가는 말이 상대 귀로 들어간다. 같은 목소리인데 나한테는 출력, 상대한테는 입력이다. **기준을 정해야만 답이 나온다.**

**아두이노 코드에서 기준은 항상 아두이노다.** LED든 버튼이든 모터든, "아두이노가 내보내냐 받아먹냐"만 보면 된다.

> GND 얘기와 같은 구조다. 5V라는 숫자도 기준(GND)이 있어야 의미가 생겼고, INPUT/OUTPUT도 기준(아두이노)이 있어야 의미가 생긴다. 임베디드에서 헷갈리는 것 절반은 "기준을 안 정하고 봐서" 생긴다.

---

## 4. Trig의 HIGH → LOW는 왜 필요한가

### 막혔던 지점

> "HIGH로 쏘라고 했으면 됐지, 왜 LOW를 주고 나서 거리를 측정하지?"

### 정리

**Trig는 "쏘는 동안 켜두는 스위치"가 아니라 "쏴!"라는 명령 한 번을 전달하는 초인종이다.**

HIGH를 주는 것 자체가 초음파를 내보내는 게 아니다. 센서 안에 자체 회로가 있어서, 10µs 이상의 HIGH 펄스를 받으면 **그때부터 알아서** 40kHz 초음파 8발을 쏜다. LOW로 내리는 건 "그만 쏴"가 아니라 명령 한 세트의 마무리다.

```cpp
digitalWrite(trig, HIGH);
delayMicroseconds(10);      // 최소 10µs는 유지해야 센서가 인식
digitalWrite(trig, LOW);    // 여기까지가 "쏴!" 명령 한 세트
```

10µs를 기다리는 이유: 센서 스펙에 **"최소 10µs는 눌러줘야 인식한다"**고 정해져 있어서. 너무 짧게 스치면 센서가 눌린 줄도 모른다.

LOW를 안 내리면 다음 측정 때 또 명령을 줄 수 없다. 계속 눌린 초인종은 다시 누를 수 없는 것과 같다. `loop()`이 계속 도니까 매번 원위치로 돌려놔야 한다.

### 실제 동작 순서

1. **아두이노**: Trig에 HIGH → 10µs → LOW (명령)
2. **센서**: 초음파 발사, Echo핀을 HIGH로 올림
3. **센서**: 반사파 수신, Echo핀을 LOW로 내림
4. **아두이노**: `pulseIn`이 2~3 사이의 시간을 잼

**2번과 3번은 코드에 나타나지 않는다.** 아두이노가 하는 일이 아니라 센서가 하는 일이기 때문. `pinMode(echo, INPUT)`으로 선언한 것이 바로 "이 핀은 내가 조종 안 함, 받아먹기만 함"이라는 뜻이다.

→ 이런 정보는 교재가 아니라 **데이터시트**에 있다. 교재는 아두이노 문법을 다루고, 센서 내부 동작은 제조사가 정한 약속이라 범위 밖이다. 앞으로 계속 반복될 패턴.

---

## 5. pulseIn과 타임아웃

```cpp
int distance = pulseIn(echo, HIGH) * 340 / 2 / 10000;
```

`pulseIn(핀, 신호)` — 지정한 핀이 해당 신호로 **유지된 시간**을 마이크로초 단위로 반환한다.

### 반사파가 안 돌아오면?

영원히 멈추지 않는다. **기본 타임아웃 1초**가 있어서, 그 안에 신호가 안 오면 `0`을 반환하고 넘어간다.

다만 두 가지 문제가 있다.

1. **거리 0cm와 측정 실패가 똑같이 `0`으로 나온다.** 구분이 안 됨.
2. **실패할 때마다 1초씩 멈춘다.** `delay(100)`으로 0.1초 주기를 설정해도, 앞이 비어 있으면 1초에 한 번씩만 돌게 된다.

### 개선

```cpp
pulseIn(echo, HIGH, 30000);   // 30ms만 대기
```

30ms면 소리가 왕복 5m 갈 시간. HC-SR04 측정 범위(최대 4m)에는 충분하다.

---

## 6. 컴파일 에러에서 배운 것

### 실제로 겪은 오타

```cpp
pulseln(echo, HIGH)     // ❌ 소문자 L
pulseIn(echo, HIGH)     // ✅ 대문자 I

pinMode(trig.OUTPUT)    // ❌ 마침표
pinMode(trig, OUTPUT)   // ✅ 쉼표
```


https://github.com/user-attachments/assets/84a05520-6834-4699-b0e8-e3bcd4f76bae


### 교훈

- 아두이노 함수는 대부분 **낙타등 표기**다. `pulseIn`, `digitalWrite`, `delayMicroseconds` — 두 번째 단어의 첫 글자는 항상 대문자.
- 에러 메시지가 답을 알려준다. `note: suggested alternative: 'pulseIn'`
- **에러는 맨 위부터 읽는다.** 컴파일러는 위에서부터 에러를 뱉는데 화면에는 마지막 것만 보인다. 앞쪽 에러 하나 때문에 뒤에 가짜 에러가 줄줄이 딸려 나오는 경우가 흔해서, 첫 번째 것을 고치면 아래가 저절로 사라지는 일이 많다.

---

## 7. 디지털 신호와 PWM

### 디지털 출력의 한계

`digitalWrite`는 두 가지만 할 수 있다.

- `HIGH` = 5V
- `LOW` = 0V

중간값이 없다. 스위치와 같다. 그래서 LED는 최대 밝기 아니면 꺼짐, 모터는 최대 속도 아니면 정지뿐이다.

### PWM의 꼼수

두 가지밖에 못 하는 부품으로 중간을 만들어내기 위해 **"빠르게 번갈아 하기"**를 쓴다.

- 시간을 **똑같은 길이의 칸**으로 잘게 썬다. 칸 하나 = **주기(period)**, 약 1/490초.
- 칸 하나 안에서 **켜져 있는 시간의 비율**을 조절한다. 이걸 **듀티비(duty cycle)**라고 한다.
- **칸의 길이는 절대 안 변한다.** 바뀌는 건 칸 안의 비율뿐.

```
25%:  ▔▁▁▁ ▔▁▁▁ ▔▁▁▁     어둡게
50%:  ▔▔▁▁ ▔▔▁▁ ▔▔▁▁     중간
90%:  ▔▔▔▁ ▔▔▔▁ ▔▔▔▁     밝게
```

**칸 하나는 켜짐 구간 + 꺼짐 구간을 합친 전체다.** 켜진 부분만 떼어놓고 보면 25%든 90%든 똑같이 네모 하나로 보여서 차이가 안 난다. 피자 한 조각이 몇 분의 일인지 알려면 판 전체를 봐야 하는 것과 같다.

```cpp
analogWrite(pin, 0~255);   // 128이면 대략 절반
```

### 왜 밝기 조절이 되는가

LED는 실제로 1/490초 단위로 깜빡이고 있다. 사람 눈이 그걸 못 따라가서 평균으로 인식할 뿐이다. 형광등이 초당 120번 깜빡여도 계속 켜진 걸로 보이는 것과 같은 원리.

**전압계로 재면 여전히 0V 아니면 5V만 나온다.** 2.5V는 실제로 존재한 적이 없다. 다만 평균 내면 2.5V처럼 행동한다.

### 용어 구분

- **펄스(pulse)** = 한 번 올라갔다 내려오는 네모 하나
- **주기(period)** = 그게 반복되는 시간 간격 (칸 하나의 폭)

주파수는 핀마다 다르다. 5·6번은 약 980Hz, 3·9·10·11번은 약 490Hz. 타이머 설정이 달라서 그렇다. 실용적으로는 신경 쓸 필요 없고, `analogWrite`에 0~255만 넣으면 된다. 주기를 의식해야 할 때는 모터에서 "삐——" 소리가 나거나 LED가 카메라에 줄무늬로 찍힐 때다.

---

## 8. 핀 구조 정리

### 핵심: 사실 나눠져 있지 않다

A0~A5도 디지털 입출력이 전부 된다. `digitalWrite(A0, HIGH)`도 작동한다. **모든 핀이 디지털인데, 그중 일부에만 추가 기능이 붙어 있는 것**뿐이다. 보드의 "ANALOG IN" 라벨은 안내판이지 다른 종족이라는 뜻이 아니다.

| 핀 | 기본 능력 | 추가 능력 |
|---|---|---|
| D0~D13 | 디지털 입출력 | 6개(`~`)만 PWM 출력 |
| A0~A5 | 디지털 입출력 | 전부 아날로그 입력 |

### PWM 핀 = D3, D5, D6, D9, D10, D11

`~` 표시가 붙어 있다. 별도의 핀 종류가 아니라 디지털 핀에 PWM 기능이 얹힌 것.

```cpp
digitalWrite(3, HIGH);   // 그냥 디지털 핀으로 사용
analogWrite(3, 128);     // PWM 핀으로 사용
```

초음파 센서 Echo를 3번에 꽂았을 때는 PWM 기능을 안 쓰고 디지털 입력으로만 쓴 것. 핀이 바뀐 게 아니라 쓰는 방식이 바뀐 것이다.

### 이름 정리 — 셋 다 같은 걸 가리킨다

- **디지털 핀** — 소속
- **PWM 핀** — 진짜 능력
- **아날로그 출력** — 그 능력으로 하는 흉내

**우노에 진짜 아날로그 출력은 없다.** PWM은 흉내다. 함수 이름이 `analogWrite`라서 헷갈리지만 이름이 사기다. 이 구분이 중요한 이유: PWM을 스피커에 넣으면 지직거리고 모터에 넣으면 삐 소리가 난다. "아날로그 출력핀"이라고만 알고 있으면 원인을 못 찾는다.

### 왜 ADC를 6개 핀에만 붙였나

**ADC 회로가 칩 안에 딱 하나뿐이다.** 핀마다 하나씩 달린 게 아니라, ADC 하나 앞에 스위치(멀티플렉서)가 붙어서 "지금은 A0, 이번엔 A3" 하고 돌려가며 쓴다. `analogRead`를 연속 호출하면 동시가 아니라 순서대로 읽힌다.

ADC는 디지털 입력 회로보다 훨씬 크고 비싸다. 14개 핀 전부에 붙이면 칩값이 오르는데, 아날로그 센서를 6개 이상 동시에 쓰는 경우는 드물어서 6개로 끝낸 것. **원가 절충.**

`~`가 6개뿐인 것도 같은 이유. 타이머가 3개뿐인데 하나가 핀 2개씩 담당한다.

---

## 9. 아날로그 입력은 무슨 기능인가

한 줄 요약: **정도(degree)를 읽는 기능.**

디지털 입력은 "있냐 없냐"만 알려준다. 아날로그 입력은 **"얼마나"**를 알려준다.

| 궁금한 것 | 필요한 입력 |
|---|---|
| 버튼이 눌렸나? | 디지털 |
| 문이 열렸나? | 디지털 |
| 방이 얼마나 밝나? | **아날로그** |
| 지금 몇 도인가? | **아날로그** |
| 손잡이를 얼마나 돌렸나? | **아날로그** |
| 발바닥을 얼마나 세게 눌렀나? | **아날로그** |

센서가 물리량을 전압으로 바꿔주고, 그 전압을 ADC가 숫자로 바꿔서 넘겨준다. 세게 밟을수록 높은 전압, 약하게 밟으면 낮은 전압.

**해상도(resolution)의 차이**로 볼 수도 있다. 디지털 입력은 2단계, 아날로그 입력은 1024단계. 세상을 몇 단계로 쪼개서 볼 수 있느냐의 문제.

### 정리 표

| | 낼 수 있는 값 | 읽을 수 있는 값 |
|---|---|---|
| 디지털 핀 | 5V / 0V 두 개 | HIGH / LOW 두 개 |
| PWM (디지털 핀 중 6개) | 5V/0V의 비율 조절 | — |
| A0~A5 | (디지털과 동일) | 0~1023, 1024단계 |

---

## 10. ADC와 DAC

- **ADC** = Analog-to-Digital Converter (아날로그 → 디지털)
- **DAC** = Digital-to-Analog Converter (디지털 → 아날로그)

컴퓨터는 숫자만 다룰 줄 아는데 현실 세계는 전부 연속적인 아날로그다. 그래서 통역이 필요하다.

`analogRead(A0)` 내부에서 벌어지는 일:

> 들어온 전압이 2.5V → 0~5V를 1024칸으로 나누면 512번째 칸 → `512` 반환

그래서 결과가 0~1023으로 나온다. **10비트 해상도** (2¹⁰ = 1024).

DAC은 그 반대로 512를 받아 실제 2.5V를 만들어낸다. 우노에는 DAC이 없어서 PWM으로 흉내내는 것. (아두이노 Due 같은 상위 보드에는 있다.)

> **왜 입력만 진짜이고 출력은 흉내인가**
> 읽는 건 이미 있는 전압을 재면 되니까 쉽고, 만드는 건 없는 전압을 새로 만들어야 해서 별도 부품이 필요하다. 우노에는 읽는 부품(ADC)은 있고 만드는 부품(DAC)은 없다.

---


https://github.com/user-attachments/assets/e3ba8f1c-da78-438c-80bc-2c5d2d5174b4



## 다음에 볼 것

- [ ] 가변저항으로 LED 밝기 조절 — `analogRead`는 0~1023, `analogWrite`는 0~255. 범위가 안 맞는 문제를 어떻게 해결하는가
- [ ] `pulseIn` 타임아웃을 실제로 적용해보기
- [ ] 아두이노 GND 핀이 3개인 이유
